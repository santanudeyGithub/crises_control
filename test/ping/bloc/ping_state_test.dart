// ignore_for_file: prefer_const_constructors
/*
import 'package:flutter_test/flutter_test.dart';
import 'package:crises_control/src/ping/bloc/bloc.dart';

void main() {
  group('PingState', () {
    test('supports value equality', () {
      expect(
        PingState(),
        equals(
          const PingState(),
        ),
      );
    });

    group('constructor', () {
      test('can be instantiated', () {
        expect(
          const PingState(),
          isNotNull,
        );
      });
    });

    group('copyWith', () {
      test(
        'copies correctly '
        'when no argument specified',
        () {
          const pingState = PingState(
            customProperty: 'My property',
          );
          expect(
            pingState.copyWith(),
            equals(pingState),
          );
        },
      );

      test(
        'copies correctly '
        'when all arguments specified',
        () {
          const pingState = PingState(
            customProperty: 'My property',
          );
          final otherPingState = PingState(
            customProperty: 'My property 2',
          );
          expect(pingState, isNot(equals(otherPingState)));

          expect(
            pingState.copyWith(
              customProperty: otherPingState.customProperty,
            ),
            equals(otherPingState),
          );
        },
      );
    });
  });
}
*/