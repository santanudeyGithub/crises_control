// ignore_for_file: prefer_const_constructors
/*
import 'package:flutter/material.dart';
import 'package:crises_control/src/ping/ping.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('PingPage', () {
    group('route', () {
      test('is routable', () {
        expect(PingListPage.route(), isA<MaterialPageRoute>());
      });
    });

    testWidgets('renders PingView', (tester) async {
      await tester.pumpWidget(MaterialApp(home: PingListPage()));
      expect(find.byType(PingListView), findsOneWidget);
    });
  });
}
*/