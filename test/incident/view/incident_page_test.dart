// ignore_for_file: prefer_const_constructors
/*
import 'package:flutter/material.dart';
import 'package:crises_control/src/incident/incident.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('IncidentPage', () {
    group('route', () {
      test('is routable', () {
        expect(IncidentsPage.route(), isA<MaterialPageRoute>());
      });
    });

    testWidgets('renders IncidentView', (tester) async {
      await tester.pumpWidget(MaterialApp(home: IncidentsPage()));
      expect(find.byType(IncidentsView), findsOneWidget);
    });
  });
}
*/