// ignore_for_file: prefer_const_constructors
/*
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('IncidentState', () {
    test('supports value equality', () {
      expect(
        IncidentState(),
        equals(
          const IncidentState(),
        ),
      );
    });

    group('constructor', () {
      test('can be instantiated', () {
        expect(
          const IncidentState(),
           
          isNotNull,
        );
      });
    });

    group('copyWith', () {
      test(
        'copies correctly '
        'when no argument specified',
        () {
          const incidentState = IncidentState(
            customProperty: 'My property',
          );
          expect(
            incidentState.copyWith(),
            equals(incidentState),
          );
        },
      );

      test(
        'copies correctly '
        'when all arguments specified',
        () {
          const incidentState = IncidentState(
            customProperty: 'My property',
          );
          final otherIncidentState = IncidentState(
            customProperty: 'My property 2',
          );
          expect(incidentState, isNot(equals(otherIncidentState)));

          expect(
            incidentState.copyWith(
              customProperty: otherIncidentState.customProperty,
            ),
            equals(otherIncidentState),
          );
        },
      );
    });
  });
}
*/